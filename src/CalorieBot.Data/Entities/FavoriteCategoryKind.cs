namespace CalorieBot.Data.Entities;

/// <summary>
/// Верхнеуровневая группа избранного. Значения задаю явно — уезжают в базу как int,
/// менять порядок после релиза уже нельзя.
/// </summary>
public enum FavoriteCategoryKind
{
    /// <summary>Обычный продукт — единственная группа, где пользователь заводит свои подкатегории.</summary>
    Product = 0,

    /// <summary>Жидкости, отслеживаются в литрах (КБЖУ хранятся на 1 л, как для «плавающей» порции на 100 г).</summary>
    Water = 1,

    /// <summary>Готовое блюдо: КБЖУ считаются автосуммой ингредиентов, а не вводятся вручную.</summary>
    Dish = 2
}
